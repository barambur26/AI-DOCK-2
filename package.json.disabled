{
  "name": "ai-dock-backend-only",
  "version": "1.0.0",
  "description": "AI Dock Backend - Temporary frontend-disabled deployment",
  "scripts": {
    "start": "cd Back && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  },
  "engines": {
    "python": ">=3.11.0"
  }
}
